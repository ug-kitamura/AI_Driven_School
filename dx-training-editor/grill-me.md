# DX Training Editor — グリルセッション記録

**実施日**: 2026-05-16  
**目的**: 4ペインワークスペースUIの仕様を詰める

---

## 認識がそろった箇所

### アプリ概要
- **アプリ名**: DX Training Editor
- **ディレクトリ名 / スラッグ**: `dx-training-editor`
- **目的**: 社内DXツールトレーニングのコンテンツを作成支援するUI
- **対象ユーザー**: 社内研修担当者1人が使う制作専用ツール（マルチユーザー・コラボ機能は不要）
- **技術スタック**: Next.js + TypeScript + Tailwind CSS + shadcn/ui（workspace-ui-kit と同一）
- **制作スタイル**: AIで骨子を生成 → このツールで読み込み・編集

### コンテンツ構造
- **階層**: シリーズ → コース → レッスン（3層）
- **シリーズ**: Pane1の見出しとして表示（ナビゲーション階層には含まない）
- **対象ツール**: Git・GitHub（スタート）→ Python・CI/CD（GitHub Actions / Jenkins）・AI活用へ順次拡張

### 4ペイン構成

| ペイン | 役割 | 主な機能 |
|---|---|---|
| **Pane 1** | シリーズ見出し＋コース一覧 | コース単位の進捗バー（X / Y レッスン完成） |
| **Pane 2** | レッスン一覧 | ステータスバッジ（未着手・作成中・完成）、CRUD（追加・削除・並び替え） |
| **Pane 3** | マークダウンエディタ | 編集モード ↔ プレビューモード切り替え、プレビュー上でも直接編集可、新規テンプレート作成・既存 `.md` ファイル読み込み |
| **Pane 4** | 画像アセットマネージャー | 4タブ構成（下記参照） |

#### Pane 4 — 画像アセットマネージャーのタブ

| タブ | 動作 |
|---|---|
| アップロード | ドラッグ&ドロップ ＋ クリップボード貼り付け（**実動作**） |
| 履歴一覧 | 過去に使用した全画像を一元表示、クリックで Pane 3 に挿入（**実動作**・全体共有スコープ） |
| AI生成 | タブUIのみ（**モック**） |
| Web検索 | タブUIのみ（**モック**） |

### YAMLフロントマター（レッスンのメタデータ）

```yaml
series: Git完全マスターシリーズ
course: Git環境構築コース
lesson: Gitのインストール手順
status: draft          # draft | in_progress | done
description: 簡潔な説明
tags: [git, setup]
estimated_minutes: 15
author: 山田太郎
```

### レッスンステータス
- **3段階**: `未着手`（draft）/ `作成中`（in_progress）/ `完成`（done）
- Pane 1 の進捗バーは `done` のレッスン数 / 総レッスン数で計算

### データ・永続化
- **モックアップ段階**: ローカルJSON（`data/` 配下）でサンプルデータを管理
- **将来の本番**: GitHubリポジトリにマークダウンファイルとして保存（YAMLフロントマター形式）
- **CRUD**: 追加・削除・並び替えはセッション内メモリのみ（保存なし）

### Cursorとの差別化ポイント
1. コース・レッスン単位の進捗バッジ・バーで現在地が一目でわかる
2. 画像アセットマネージャーでスクショを2アクションで本文に挿入できる
3. YAMLフロントマターがレッスンの所属（シリーズ・コース）を自己記述する
4. 新規レッスン作成時に階層情報・著者が自動入力されるテンプレートが出る

---

## 解像度が上がった箇所

- **Pane 3 の編集体験**: マークダウンをAIで生成し外部から持ち込む前提。エディタはゼロから書く場所ではなく「受け取って磨く」場所
- **画像管理の痛み**: 毎回ファイルパスを手打ちする・スクショの整理ができていない → Pane 4 で解消
- **進捗把握の痛み**: どのレッスンが穴か分からない → Pane 1 + Pane 2 の2層バッジで解消
- **画像スコープ**: 履歴は全体共有（シリーズ・コースをまたいで再利用可）

---

## 詰め切れていない箇所

1. **カラースキーム・ブランディング**: アプリ名は決定したが、テーマカラーは未決
2. **Pane 3 の編集/プレビュー切り替えUX**: トグルボタン方式か、タブ方式か、Notion風インライン切り替えか
3. **画像の挿入形式**: クリック時に `![alt](path)` を挿入するのか、カーソル位置に挿入するのか
4. **ファイル読み込みのスコープ**: 既存 `.md` を読み込む際、ファイルシステムから直接か、URLペーストか
5. **Pane 1 の折りたたみ**: workspace-ui-kit には Pane1 トグルがある。DX Training Editor でも折りたたみが必要か
6. **確認問題のデータ構造**: レッスン末尾の確認問題はYAML内か、本文マークダウン内に独自記法で書くか

---

## 次のステップの候補

1. **プラン化**: workspace-ui-kit の各コンポーネント（`Workspace.tsx`、`PositionPane.tsx` 等）をどう置き換えるか設計する
2. **実装開始**: `data/` のサンプルJSONをDXトレーニング用に書き換えるところから着手する
3. **追加グリル**: 詰め切れていない箇所（特に2〜6）をさらに深掘りする

---

## 補足・参考情報

### workspace-ui-kit との対応関係（参考）

| workspace-ui-kit（採用管理） | DX Training Editor |
|---|---|
| `PositionPane`（部署/ポジション一覧） | Pane 1（シリーズ見出し＋コース一覧） |
| `CandidateListPane`（候補者リスト） | Pane 2（レッスン一覧） |
| `CandidateDashboardPane`（候補者ダッシュボード） | Pane 3（マークダウンエディタ） |
| `CandidateDetailPane`（選考ステージ詳細） | Pane 4（画像アセットマネージャー） |
| `data/*.json`（サンプルデータ） | `data/*.json`（シリーズ・コース・レッスンのサンプル） |

### コンテンツの流れ（制作ワークフロー）
```
AIスキル / Python スクリプト
    ↓ マークダウン生成
DX Training Editor の Pane 3 へ読み込み
    ↓ 編集・画像挿入（Pane 4）
    ↓ ステータス更新（Pane 2）
将来: GitHub リポジトリへ .md ファイルとして保存
```

### マークダウンエディタライブラリ候補（未選定）
- `@uiw/react-md-editor` — シンプル、MIT ライセンス
- `@monaco-editor/react` — VS Code 相当の機能、重め
- `react-markdown` + `textarea` — 最軽量、自前でプレビュー実装
