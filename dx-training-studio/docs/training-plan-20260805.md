# DXトレーニング コンテンツ計画（シリーズ → コース → レッスン）

- 作成日: 2026-08-05
- 対象: DXトレーニング 自己学習コンテンツ ＋ ハンズオントレーニング第1弾
- 決定手段: 対話セッション（本書末尾に決定の根拠と却下案を記載）
- ステータス: 構成確定 / ハンズオン題材確定 / 検証待ちの事項あり（→「未決事項」参照）
- 前版: `training-plan-20260804.md`（本書が置き換える）

前版からの主な変更は、**ハンズオンの題材と進め方の全面的な再設計**、**F2F キックオフと VSCode 環境準備の追加**。

---

## 1. 前提

### 1.1 目的

システム・ソフトのメンバにツール開発を促し、業務効率を上げる。

### 1.2 受講者像

エンジニアリング部署の新入社員・中途採用社員。

| 項目 | 前提 |
|---|---|
| プログラミング | 多少の知識はある（変数・制御構造の説明は不要） |
| Python | 初心者 |
| Git / GitHub | 初心者 |
| AI | 社内チャットツールや ChatGPT で調べ物をしたことがある程度 |
| エディタ | VSCode を使ったことがない前提 |
| Markdown | 前提としない（読めれば足りる範囲でのみ扱う） |

### 1.3 ゴールの定義

**2時間（1h × 2回）で、必ず動くツールが1つ完成する体験**を全員に届ける。

加えて、ハンズオンでは以下の2点を成立させる。

- **AI を使ってよい。ただし発表で自分のコードを自分で説明できること。** AI に丸投げして理解せずに済ませる導線を、運用ルールで塞ぐ
- **応用課題の手法は教えない。** 各自が数日かけて工夫する。運営は解法を配らない

> **前版からの変更**: 「入力データだけ受講者自身の業務データを使い、自分事として取り組めるようにする」を削除した。理由は 11.2 を参照。

### 1.4 分量方針

| 項目 | 値 |
|---|---|
| 総レッスン数 | 27（集合 1 ＋ 自己学習 24 ＋ ハンズオン 2） |
| 1レッスン | 10〜15分 |
| 自己学習の総量 | 340分（5時間40分。上限6時間） |
| 自己学習期間 | 3週間 |
| 集合回 | **3回** — キックオフ（L01）＋ ハンズオン2日 |
| ハンズオン | 1h × 2回、間隔 3〜4日（自己学習時間には含めない） |
| ハンズオン定員 | 8人（4組） |

- 必須／任意の2層分けはしない。自己学習24レッスンすべてが必須
- ハンズオンで直接使わない基礎・概念・セットアップも厚くカバーする
- **全レッスンに5分以内の「やってみる」演習を入れる**
- **手順の説明にはスクリーンショットや図を載せる。** 文章だけで済ませない
- **小演習には折りたたんだ解答例を1行つける。** 完成コード一式を渡すのはハンズオンのみ

解答例は `<details>` で折りたたみ、受講者が自分のタイミングで答え合わせできるようにする。

```markdown
### やってみる
（課題）

<details>
<summary>解答例</summary>

（1行の解答例）

</details>
```

### 1.5 設計原則: シリーズ内で完結させる

**あるシリーズのトピックは、たとえリードタイムがかかっても、そのシリーズ内で完結させる。**

社内では Git のインストール、社内 GitHub のアカウント、VSCode、uv のいずれも申請が必要で、承認まで日数がかかる。これらを「時間がかかるから」という理由で先頭のシリーズに集めない。

| 申請 | 置き場所 | 根拠 |
|---|---|---|
| Git のインストール | ③ Gitシリーズ / L06 | Git のトピック |
| 社内 GitHub のアカウント | ④ GitHubシリーズ / L10 | GitHub のトピック |
| uv（Python環境） | ⑤ Pythonシリーズ / L18 | Python のトピック |
| **VSCode** | **① はじめにシリーズ / L02** | **Git にも Python にも効き、どのシリーズにも属さない** |

VSCode だけが例外なのは、横断的なツールだからであって、リードタイムが長いからではない。

**リードタイムへの対処は、レッスン配置ではなく F2F キックアップの口頭案内で行う。**「いつ動くべきか」は口頭が向き、「どう操作するか」はレッスンが向く（→ 11.10）。

---

## 2. 全体構造（シリーズ → コース → レッスン）

```
DXトレーニング
│
├─ ① はじめにシリーズ  [start]  ......................... 2レッスン / 25分
│   ├─ DX入門コース  [intro]  《lecture》 ☆
│   │   └─ L01 トレーニングの進め方                       10分
│   └─ 環境準備コース  [setup]  《self-study》
│       └─ L02 VSCodeのインストールと初期設定             15分
│
├─ ② AIシリーズ  [ai]  ................................. 1レッスン / 15分
│   └─ AI基礎コース  [basics]  《self-study》
│       └─ L03 社内AIチャットの使い方                     15分
│
├─ ③ Gitシリーズ  [git]  ............................... 6レッスン / 85分
│   ├─ Git概念コース  [concepts]  《self-study》
│   │   ├─ L04 バージョン管理ってなに？                   15分
│   │   └─ L05 Gitの三大エリア                           15分
│   ├─ Git環境構築コース  [setup]  《self-study》
│   │   └─ L06 Gitのインストールと初期設定                15分
│   └─ Git基本操作コース  [basics]  《self-study》
│       ├─ L07 最初のコミット（.gitignore を含む）        15分
│       ├─ L08 履歴と差分を読む                          15分
│       └─ L09 変更を取り消す                            10分
│
├─ ④ GitHubシリーズ  [github]  ......................... 6レッスン / 85分
│   ├─ GitHub入門コース  [intro]  《self-study》
│   │   ├─ L10 GitHubとは・アカウント申請                 10分
│   │   └─ L11 リポジトリを作る / clone                   15分
│   ├─ リモート連携コース  [remote]  《self-study》
│   │   └─ L12 push と pull（README.md を題材に）         15分
│   └─ ブランチとPRコース  [branch-pr]  《self-study》
│       ├─ L13 ブランチを切る・切り替える                 15分
│       ├─ L14 push して Pull Request を出す              15分
│       └─ L15 レビューとマージ・コンフリクト解消          15分
│
├─ ⑤ Pythonシリーズ  [python]  ........................ 9レッスン / 130分
│   ├─ Python概要コース  [overview]  《self-study》
│   │   └─ L16 Pythonとは何か                            10分
│   ├─ Python環境構築コース  [setup]  《self-study》
│   │   ├─ L17 Python環境のセットアップ（uv）             15分
│   │   └─ L18 はじめてのスクリプトを動かす               15分
│   ├─ Python基礎コース  [basics]  《self-study》
│   │   ├─ L19 変数と型                                  15分
│   │   ├─ L20 条件分岐とループ                          15分
│   │   └─ L21 リストと辞書                              15分
│   └─ Python中級コース  [intermediate]  《self-study》
│       ├─ L22 関数とモジュール分割                       15分
│       ├─ L23 ファイル読み書き（CSV / JSON）             15分
│       └─ L24 外部ライブラリを使う（pandas 超基礎）       15分
│
└─ ⑥ ツール開発実践シリーズ ☆  [build]  ................ 3レッスン / 10分＋120分
    ├─ ハンズオン事前準備コース  [prep]  《self-study》
    │   └─ L25 ハンズオンの準備                           10分
    └─ ハンズオン当日コース  [workshop]  《hands-on》 ☆
        ├─ L26 組分けスクリプトを作る（1日目）             60分
        └─ L27 応用課題に挑む（2日目）                     60分
```

凡例:
- `[slug]` … 公開URLに使うスラッグ
- `《lecture》` `《self-study》` `《hands-on》` … コース `.meta.json` の `format`
- `☆` … **集合形式のコース**（`lecture` と `hands-on` の両方に付く）

---

## 3. 曼陀羅構成

### 3.1 コース依存グラフ

```
             ┌───────────────────────────────────────┐
             │ ① はじめにシリーズ                     │
             │      ┌──────────────────────────┐     │
       ┌─────┼──────│ DX入門コース ☆            │─────┼─────┐
       │     │      └─────────────┬────────────┘     │     │
       │     │                    ▼                  │     │
       │     │      ┌──────────────────────────┐     │     │
       │     │      │ 環境準備コース（VSCode）  │     │     │
       │     │      └─────────────┬────────────┘     │     │
       │     └────────────────────┼──────────────────┘     │
       ▼                          ▼                        ▼
┌───────────────┐      ┌──────────────────┐    ┌────────────────────┐
│ ② AIシリーズ   │      │ ③ Gitシリーズ    │    │ ⑤ Pythonシリーズ    │
│ ┌───────────┐ │      │ ┌──────────────┐ │    │ ┌────────────────┐ │
│ │AI基礎コース│ │      │ │ 概念コース    │ │    │ │ 概要コース      │ │
│ └─────┬─────┘ │      │ └──────┬───────┘ │    │ └───────┬────────┘ │
└───────┼───────┘      │        ▼         │    │         ▼          │
        │              │ ┌──────────────┐ │    │ ┌────────────────┐ │
        │              │ │環境構築コース │ │    │ │ 環境構築コース  │ │
        │              │ └──────┬───────┘ │    │ └───────┬────────┘ │
        │              │        ▼         │    │         ▼          │
        │              │ ┌──────────────┐ │    │ ┌────────────────┐ │
        │              │ │基本操作コース │ │    │ │ 基礎コース      │ │
        │              │ └──────┬───────┘ │    │ └───────┬────────┘ │
        │              └────────┼─────────┘    │         ▼          │
        │                       │              │ ┌────────────────┐ │
        │                       ▼              │ │ 中級コース      │ │
        │        ┌──────────────────────┐      │ └───────┬────────┘ │
        │        │ ④ GitHubシリーズ      │      └─────────┼──────────┘
        │        │ ┌──────────────────┐ │                │
        │        │ │ GitHub入門コース  │ │                │
        │        │ └────────┬─────────┘ │                │
        │        │          ▼           │                │
        │        │ ┌──────────────────┐ │                │
        │        │ │ リモート連携コース │ │                │
        │        │ └────────┬─────────┘ │                │
        │        │          ▼           │                │
        │        │ ┌──────────────────┐ │                │
        │        │ │ ブランチとPRコース │ │                │
        │        │ └────────┬─────────┘ │                │
        │        └──────────┼───────────┘                │
        │                   │                            │
        └─────────┬─────────┴──────────────┬─────────────┘
                  ▼                        ▼
       ┌───────────────────────────────────────────┐
       │ ⑥ ツール開発実践シリーズ                    │
       │   ハンズオン事前準備コース 《self-study》     │
       │              ▼                            │
       │   ハンズオン当日コース ☆ 《hands-on》        │
       └───────────────────────────────────────────┘
```

### 3.2 シリーズ間の辺（`cross_series_prev` / `cross_series_next`）

| # | From（コース） | To（コース） | シリーズ間ペア |
|---|---|---|---|
| 1 | ① / **DX入門** | ② / AI基礎 | start → ai |
| 2 | ① / **環境準備** | ③ / Git概念 | start → git |
| 3 | ① / **DX入門** | ⑤ / Python概要 | start → python |
| 4 | ③ / Git基本操作 | ④ / GitHub入門 | git → github |
| 5 | ② / AI基礎 | ⑥ / ハンズオン事前準備 | ai → build |
| 6 | ④ / ブランチとPR | ⑥ / ハンズオン事前準備 | github → build |
| 7 | ⑤ / Python中級 | ⑥ / ハンズオン事前準備 | python → build |

**環境準備コース（VSCode）を経由するのは Git だけ。** AI と Python は DX入門コースから直接分岐する。

| シリーズ | VSCode | 根拠 |
|---|:---:|---|
| ② AI | 不要 | 社内AIチャットはブラウザで完結する。VSCode + GitHub Copilot は将来の課題で、現時点では扱わない |
| ③ Git | **必須** | 実務では Git の操作をコマンドより VSCode から行うことが多い |
| ⑤ Python | あると便利／必須ではない | エディタは何でもよい。VSCode を前提にすると、承認待ちで Python まで止まる |

送り元コースが違っても**シリーズペアが異なれば辺は張れる**ため、この形は規則に抵触しない。

### 3.3 チェック結果

`references/mandala-guide.md` のチェック項目に対する判定。

| 規則 | 判定 |
|---|---|
| 循環がないか | ✓ `start → {ai, git, python} → github → build` の一方向のみ。逆流する辺がない |
| ゴールに到達できるか | ✓ 全6シリーズが辺をたどって `build/workshop` に至る。Git は GitHub 経由、他は直接 |
| 行き止まりがないか | ✓ ゴール以外の全コースが出口の辺を持つ。**環境準備コースは Git概念への辺**を持つため行き止まりにならない |
| 到達不能なコースがないか | ✓ 起点 `start/intro` 以外の全コースが入口の辺を持つ（シリーズ内の直列鎖を含む） |
| 辺の重複がないか | ✓ 7本すべて異なるシリーズペア。`start→ai` と `start→python` は DX入門から、`start→git` は環境準備から出るが、**ペアが異なるので重複ではない** |
| シリーズ内は直列鎖のみか | ✓ 全6シリーズがコース並び順の一本道。**はじめにシリーズも `DX入門 → 環境準備` の直列** |
| 1コースから同一ターゲットへ複数辺を出していないか | ✓ DX入門が出す2本（AI・Python）は宛先シリーズが異なる。build/prep への3本も送り元シリーズが全て異なる |

**経由による暗黙の必須化**: Gitシリーズは build への直接の辺を持たないが、`Git基本操作 → GitHub入門` を経由するため**全Gitコースが build の前提として必須**になる。辺1本制約の下で辺を増やさずに必須化できている。

**環境準備コースも同じ仕組みで必須になる。** Python や AI から build へ直行できるように見えるが、build に入るには合流する3本すべての元コースが完了している必要がある。GitHub 側の経路が `環境準備 → Git → GitHub → build` を通るため、**VSCode を飛ばして修了することはできない**。

**AIシリーズの位置づけ変更**: 前版では AI基礎 → build の辺は「おまけを飛ばさせないための辺」だった。本版ではハンズオンで AI 使用可と決めたため、**この辺は実質的な前提**になった。辺そのものは変更していないが、意味づけが強くなっている。

### 3.4 進行ルール

- あるコースに入るには、そこへ合流する**全ての辺の元コースが完了済み**である必要がある
- その範囲内であれば、受講者はどのブランチから進めても構わない（順序は受講者に任せる）
- 全ブランチの完了を求めるが、**ハンズオン参加の強制ゲートにはしない**。未完了でも希望者は参加でき、その時点でできる限りのことをしてもらう

### 3.5 VSCode の承認待ちに何ができるか

VSCode は社内独自の申請プロセスが必要で、承認まで日数がかかる。**この待ち時間に AI（15分）と Python（130分）を進められる**のが、分岐を DX入門コースから出した理由。

```
キックオフ（L01）で申請 ──┐
                          │  〜 承認待ち 〜
   ② AIシリーズ      15分 │  ← この間に進められる
   ⑤ Pythonシリーズ 130分 │
                          ▼
   ① 環境準備（VSCode）   → ③ Gitシリーズ → ④ GitHubシリーズ
```

**環境準備コースを3方向すべての手前に置く案は却下した。** VSCode が全ブランチのゲートになり、**承認待ちの数日間、受講者が何もできなくなる**。3週間の学習期間の頭で145分ぶんの進行機会を失う。

Git 側は VSCode の承認を待つことになるが、**Git 自体もインストール申請が必要**（L06）なので、どのみち待ちが発生する。両者を並行して待たせるほうが期間の使い方として効率がよい。

---

## 4. シリーズ仕様

### ① はじめにシリーズ `start`

| 項目 | 内容 |
|---|---|
| ゴール | 曼陀羅の見方と進め方を理解し、自分が取り組む順番を決められる／VSCode が使える状態になる |
| 対象 | 受講者全員（最初に必ず通る） |
| コース数 / レッスン数 / 時間 | 2 / 2 / 25分 |
| 前提 | なし |
| 分岐 | **DX入門コースから AI・Python へ。環境準備コースから Git へ**（→ 3.2、3.5） |

**コースを2つに分けた理由**: L01 はトレーナーが F2F で説明する `lecture`、L02（VSCode）は自己学習。**学習形態はコース単位で一貫させる**という既定方針に従い、形態が違う2つを同じコースに同居させない。

**L01 はトレーナーが F2F で説明する。** その場で、以下を**早めに済ませておくよう口頭で案内する**。いずれも申請にリードタイムがあり、着手が遅れると学習期間の終盤で詰まるため。

- VSCode のインストール（社内独自の申請プロセスが必要）
- Git のインストール申請
- 社内 GitHub のアカウント申請
- uv（Python環境）の申請

**この案内はレッスン本文には書かない。** Git の話は Gitシリーズ、Python の話は Pythonシリーズで扱う。L01 に各シリーズの事前準備を並べると、**進め方のレッスンが環境構築の目次になってしまう**（→ 1.5、11.10）。

**VSCode だけが例外的にここに置かれる。** Git にも Python にも効く横断的なツールで、どのシリーズにも属さないため。

### ② AIシリーズ `ai`

| 項目 | 内容 |
|---|---|
| ゴール | 社内AIチャットツールの基本操作ができ、詰まったときに聞ける |
| 位置づけ | **ハンズオンの前提**（前版では「おまけ」） |
| レッスン数 / 時間 | 1 / 15分 |
| 前提 | DX入門コース（**VSCode は不要**） |
| 今後 | コンテンツを育てていく想定。シリーズを独立させたのはそのため |

**VSCode を前提にしない。** 社内AIチャットはブラウザで完結する。**VSCode + GitHub Copilot はゆくゆく扱いたいが、現時点では踏み込まない。** 将来このシリーズを育てるときに、環境準備コースからの辺を張り直すか検討する（現状 `start→ai` の辺は DX入門から出ている）。

**設計意図の更新**: ハンズオンの応用課題は手法を教えず、数日間の自主考察に委ねる。その間の主要な調べ手段が AI になるため、**AI に聞けることが応用課題の前提条件**になった。レッスン数は1本のままだが、役割は前版より重い。

### ③ Gitシリーズ `git`

| 項目 | 内容 |
|---|---|
| ゴール | ローカルで変更を記録・確認・取り消しでき、生成物をコミットから除外できる |
| レッスン数 / 時間 | 6 / 85分 |
| 前提 | **環境準備コース（VSCode が必要）** |
| 範囲外 | ブランチ（GitHubシリーズで扱う） |

**設計意図**: ほとんどの操作は結局 GitHub とセットで行うため、Gitシリーズは**ローカル操作の基礎に絞って軽くする**。

**教え方**: **CLI を主とし、同じ操作を VSCode でどうやるかを併記する。** 実務では VSCode から Git を操作することが多いが、コマンドを先に見せないと三大エリア（L05）の概念と操作が対応しない（→ 11.12）。

**本版での追加**: ハンズオンの成果物は Python スクリプトであり、**スクリプトが生成する JSON はコミットしない**方針にした。これを実行するには `.gitignore` が必要になるため、L07「最初のコミット」に含めた。

### ④ GitHubシリーズ `github`

| 項目 | 内容 |
|---|---|
| ゴール | ブランチを切って PR を出し、レビューを経てマージできる |
| レッスン数 / 時間 | 6 / 85分 |
| 前提 | Git基本操作コース（commit できないと push できないため） |

**設計意図**: 「ブランチを切る → push → PR → マージ」が1コース内で地続きになるよう、ブランチをこのシリーズに置いた。

**本版での役割強化**: ハンズオンの進行そのものに PR → レビュー → 承認 → マージ を組み込んだため、L13〜L15 は**当日の必須スキル**になった。前版では成果物を「置く」だけだったが、本版では**ペア相互レビューの手段**として使う。

**社内 GitHub のアカウント申請は L10 に置く**（→ 1.5）。承認までのリードタイムが特に長いため、キックオフで早めの申請を口頭案内する。

### ⑤ Pythonシリーズ `python`

| 項目 | 内容 |
|---|---|
| ゴール | ファイルを読んで加工し、結果を別形式で出力するスクリプトを書ける |
| レッスン数 / 時間 | 9 / 130分 |
| 前提 | DX入門コース（**VSCode はあると便利だが必須ではない**） |

**VSCode を前提にしない。** エディタは何でもよい。VSCode を前提にすると、その承認待ちで Python まで止まる。実際には多くの受講者が VSCode を使うが、**レッスンの手順を特定のエディタに縛らない**。

**環境構築は uv を使う。** Anaconda は2026年末まで社内で使用できるが、**主流が uv に移りつつある**ため、本トレーニングでは uv に統一する（→ 11.13）。

**設計意図**: 中級コースの3本は、ハンズオンの完成物から逆算した構成にしている。

| レッスン | ハンズオンでの用途 |
|---|---|
| L22 関数とモジュール分割 | 読み込み・除外・組分け・出力を関数に切る |
| L23 ファイル読み書き（CSV / JSON） | 入力CSVを読み、結果を JSON で書き出す |
| L24 外部ライブラリを使う（pandas） | CSV の読み込みと集計を pandas でも書ける（任意） |

### ⑥ ツール開発実践シリーズ `build` ☆

| 項目 | 内容 |
|---|---|
| ゴール | 課題を解決する Python スクリプトを1つ完成させ、PR とレビューを経て自分のリポジトリにマージする |
| レッスン数 / 時間 | 3 / 10分（事前）＋ 60分×2（当日） |
| 前提 | AI基礎・GitHub ブランチとPR・Python中級 の3本が合流 |
| 定員 | 8人（4組） |

**命名の意図**: 「卒業試験」等ではなく **「ツール開発実践」**。ゴールは「作れた実感」であって評価ではないため、試験という含意を避けた。

#### 6.1 ハンズオンの題材

**課の10人を週ごとに3組に分ける組分けスクリプト。**

| Part | 内容 | 実施 |
|---|---|---|
| Part 1 | 休暇予定CSVを読み、休暇者を除いて3組に分け、JSON で出力する | 1日目 |
| Part 2 | 4週分を生成し、前週と同じペアが出ないようにする | 2日目（間に3〜4日の自主考察） |

**Part 2 の想定解法**は「シャッフルして、条件を満たすまでやり直す」。最適化アルゴリズムではなく、**評価関数を書いて満たすまで試す**という考え方を身につけさせる。リトライ上限を入れないと無限ループになるため、「条件を厳しくすると解が存在しなくなる」ことを実地で知る場面になる。

ただし **解法は指定しない。** 手法はレッスンで扱わず、各自の工夫に委ねる。高等数学を使う解も、素朴なリトライ解も認める。解法が発散することが、全員発表の価値を作る。

**課題は1日目の開始まで伏せる。** リポジトリには骨組み（README とサンプルCSV）のみを置き、課題文は当日配布する。

#### 6.2 ハンズオンの進め方

**ペアは当日その場で決める。** 運営が用意した**ペア決めスクリプトを冒頭で実行してデモ**し、その結果でペアを確定する。講師が恣意的に決めるより納得感があり、**今日作るのと同じ種類のツールが、その場の実データ（参加者名簿）で動く**ところを見せられる。

**このスクリプトは受講者に配布する。ただし範囲を絞る。**

| 要素 | 配布物に含む | Part 1 |
|---|:---:|---|
| シャッフルして分割する | ○ | 発想を参考にできる |
| CSV を読む | — | 自力（L23） |
| 条件で除外する | — | 自力（L20） |
| JSON で出力する | — | 自力（L23） |
| リトライで条件を満たす | — | **Part 2。一切漏れない** |

CSV読込・除外・JSON出力は**ペア決めには本来不要**なので、無理なく排除できる。Part 2 の想定解法（評価関数を書いて満たすまで試す）は一回きりのペア決めに要らないため、本当の課題は守られる。

**2人一組。ただしペアは「相互レビューの相手」であり、共同で実装はしない。** 各自が自分の個人リポジトリを持ち、**相方を collaborator に招待してレビューできるようにする**。

```
Aさんのリポジトリ                    Bさんのリポジトリ
  feature ブランチ                     feature ブランチ
      │ PR                                 │ PR
      ▼                                    ▼
   B がレビュー・承認                   A がレビュー・承認
      │                                    │
      ▼ A がマージ                         ▼ B がマージ
    main                                 main
```

レビュー時、**相方は「なぜこう書いたか」を1つ質問し、答えられたら承認する。** これが「自分のコードを自分で説明できる」ことの検証になり、発表のリハーサルにもなる。

**承認は仕組みで強制しない。branch protection は使わない。** 設定の説明に当日の時間を取られるうえ、レビューを飛ばす動機が受講者側にない。運用ルールとして伝えるだけで足りる。

**招待は collaborator で行う。** 個人リポジトリの collaborator は write 権限になり、相方も自分のリポジトリに push できる状態になる。ただし相互レビューの相手として招く関係なので実害はなく、権限を read に絞るためだけに org 配下へ移す等の手間はかけない。

**招待は当日、Part 1 の実装時間の中で行う。** ペアが当日決まるため事前には送れない。操作は Settings → Collaborators から username で追加するだけで、当日その場で説明する。**招待の往復（送信 → 相手が承認）は28分の実装時間に吸収され**、PR レビュー（38分〜）には間に合う。

#### 6.3 ハンズオンの時間割

```
1日目 60分  ─────────────────────────────────────────────
00-10  課題公開・骨組み説明
       ペア決めスクリプトのデモ（実行してペア確定）→ スクリプトを配布
       collaborator 招待のやり方を説明
10-38  Part 1 実装（各自）  CSV読込 → 休暇者除外 → 3組に分ける → JSON出力
       ※ この時間内に相方を collaborator に招待し、承認まで済ませておく
38-50  PR 作成 → 相方がレビュー（「なぜこう書いたか」を1つ質問）→ 承認 → マージ
50-55  main で動作確認 ── ここで動くツールが完成
55-60  応用課題（Part 2）の説明

        〜 3〜4日 〜  仲間と相談OK / AIに聞くのもOK / 解法は配らない

2日目 60分  ─────────────────────────────────────────────
00-05  進捗確認
05-30  Part 2 実装（25分）
30-38  PR → レビュー → 承認 → マージ
38-54  発表  各自2分 × 8人（自分のコードを自分で説明する）
54-60  まとめ
```

**Part 1 まで到達すれば動くツールが完成する。** Part 2 は上積みなので、応用に届かなかった人も手ぶらでは帰らない。

**発表は未完成でも構わない。** できたところまでを話し、何に詰まったかを共有する。設計原則の「未完成での参加を許容することを明示する」（第11回）に沿う運用で、**詰まった箇所の共有そのものが他の参加者の学びになる**。

---

## 5. コース仕様

| # | シリーズ | コース | slug | format | レッスン | 時間 | 受講対象 |
|---|---|---|---|---|---|---|---|
| 1 | はじめに | DX入門 ☆ | `start/intro` | **lecture** | 1 | 10分 | 受講者全員 |
| 2 | はじめに | 環境準備 | `start/setup` | self-study | 1 | 15分 | VSCode を使ったことがない人 |
| 3 | AI | AI基礎 | `ai/basics` | self-study | 1 | 15分 | AIチャットを業務で使ったことがない人 |
| 4 | Git | 概念 | `git/concepts` | self-study | 2 | 30分 | バージョン管理を全く知らない開発者 |
| 5 | Git | 環境構築 | `git/setup` | self-study | 1 | 15分 | Gitをインストールしていない人 |
| 6 | Git | 基本操作 | `git/basics` | self-study | 3 | 40分 | Gitを入れたがコマンドを打ったことがない人 |
| 7 | GitHub | 入門 | `github/intro` | self-study | 2 | 25分 | 社内GitHubアカウントを持っていない人 |
| 8 | GitHub | リモート連携 | `github/remote` | self-study | 1 | 15分 | ローカルのcommitはできる人 |
| 9 | GitHub | ブランチとPR | `github/branch-pr` | self-study | 3 | 45分 | push/pull ができる人 |
| 10 | Python | 概要 | `python/overview` | self-study | 1 | 10分 | Pythonを書いたことがない人 |
| 11 | Python | 環境構築 | `python/setup` | self-study | 2 | 30分 | Pythonを入れていない人 |
| 12 | Python | 基礎 | `python/basics` | self-study | 3 | 45分 | Pythonを動かせる状態の人 |
| 13 | Python | 中級 | `python/intermediate` | self-study | 3 | 45分 | 基礎文法を一通り終えた人 |
| 14 | ツール開発実践 | ハンズオン事前準備 | `build/prep` | self-study | 1 | 10分 | ハンズオン参加予定者 |
| 15 | ツール開発実践 | ハンズオン当日 ☆ | `build/workshop` | hands-on | 2 | 120分 | ハンズオン参加者（定員8人） |

---

## 6. レッスン仕様

各レッスンは「学習目標 → 本文 → やってみる（5分以内） → まとめ → 確認問題」で構成する。
以下は**大まかな構成まで**（詳細な本文設計・必要画像の洗い出しはレッスン草稿作成スキルの担当）。

### ① はじめにシリーズ

| ID | レッスン | slug | 時間 | 学習目標 | やってみる |
|---|---|---|---|---|---|
| L01 | トレーニングの進め方 ☆ | `start/intro/how-to-learn` | 10分 | 曼陀羅の見方と進め方が分かる／**教わるのではなく自ら学ぶ姿勢で臨むことを理解する** | 曼陀羅を見て、自分が取り組む順番を決める |
| L02 | VSCodeのインストールと初期設定 | `start/setup/vscode` | 15分 | VSCode を導入し、フォルダを開いてファイルを編集できる | 練習用フォルダを開いてファイルを1つ作る |

> L01 では「運営は代わりにツールを作らない。分からないことは自分で調べ、AI に聞く」という前提を明示する。ハンズオンの応用課題で手法を教えない方針とも直結するので、ここでの共有が効く。
>
> **「やってみる」が学習目標を検証する形になっている。** 曼陀羅を読まなければ順番は決められないため、演習をこなすこと自体が「曼陀羅の見方が分かる」の確認になる。順番を自分で決めるという行為が、「自ら学ぶ姿勢」の最初の実践にもなる。
>
> **L01 はトレーナーが F2F で説明する。** レッスン本文は、その場の説明資料と、後から読み返すための記録を兼ねる。各種申請の案内は**口頭で行い、本文には書かない**（→ 4章 ①、11.10）。
>
> L02 は VSCode の申請自体には触れない（キックオフで案内済みのため）。承認後のインストールと初期設定を扱う。

### ② AIシリーズ

| ID | レッスン | slug | 時間 | 学習目標 | やってみる |
|---|---|---|---|---|---|
| L03 | 社内AIチャットの使い方 | `ai/basics/chat-tool` | 15分 | 社内AIチャットを開いて質問できる／社内情報の扱いの注意点が分かる | エラーメッセージを1つ貼って意味を聞いてみる |

### ③ Gitシリーズ

| ID | レッスン | slug | 時間 | 学習目標 | やってみる |
|---|---|---|---|---|---|
| L04 | バージョン管理ってなに？ | `git/concepts/what-is-version-control` | 15分 | バージョン管理が必要な理由を3つ挙げられる | 手元の「最終版2」ファイルを探して数えてみる |
| L05 | Gitの三大エリア | `git/concepts/three-areas` | 15分 | 作業ツリー／ステージ／リポジトリの違いを説明できる | 図の空欄にエリア名を埋める |
| L06 | Gitのインストールと初期設定 | `git/setup/install` | 15分 | **インストール申請を出せる**／Gitを入れ、名前とメールを設定できる | 申請を出す →（承認後）`git --version` と `git config --list` を実行する |
| L07 | 最初のコミット | `git/basics/first-commit` | 15分 | ファイルを add して commit できる／**生成物を `.gitignore` で除外できる** | 出力ファイルを `.gitignore` に追加し、`status` に出ないことを確認する |
| L08 | 履歴と差分を読む | `git/basics/log-and-diff` | 15分 | `status` `log` `diff` で状態を確認できる | 1行編集して差分を表示する |
| L09 | 変更を取り消す | `git/basics/undo` | 10分 | コミット前の変更を安全に戻せる | わざと壊して `restore` で戻す |

> L06 は冒頭に**インストール申請**を置く。承認まで数日かかるため、レッスンの分量は15分だが**実時間は数日をまたぐ**。早めの着手は F2F キックオフでの口頭案内が促す。
>
> L07 の `.gitignore` は、ハンズオンで「スクリプトはコミットするが、生成された JSON はコミットしない」を実行するために必要。単独レッスンにはせず、コミット対象を決める文脈の中で扱う。
>
> **L06〜L09 は CLI を主とし、同じ操作を VSCode でどうやるかをスクリーンショットで併記する**（→ 11.12）。

### ④ GitHubシリーズ

| ID | レッスン | slug | 時間 | 学習目標 | やってみる |
|---|---|---|---|---|---|
| L10 | GitHubとは・アカウント申請 | `github/intro/account` | 10分 | GitHubの役割が分かる／**社内GitHubのアカウント申請を出せる** | 申請を出す →（承認後）サインインしてプロフィールを確認する |
| L11 | リポジトリを作る / clone | `github/intro/repository` | 15分 | リポジトリを作成し、手元に clone できる | 練習用リポジトリを作って clone する |
| L12 | push と pull | `github/remote/push-pull` | 15分 | ローカルのコミットを push し、変更を pull できる | **README.md を書いて push し、GitHub 上での表示を確認する** |
| L13 | ブランチを切る・切り替える | `github/branch-pr/branch` | 15分 | ブランチを作成・切替でき、なぜ切るかを説明できる | 作業用ブランチを作って切り替える |
| L14 | push して Pull Request を出す | `github/branch-pr/pull-request` | 15分 | ブランチを push して PR を作成できる | 自分のリポジトリで PR を1本作る |
| L15 | レビューとマージ・コンフリクト解消 | `github/branch-pr/review-merge` | 15分 | レビューコメントを付け、承認してマージできる／衝突を解消できる | 自分の PR にコメントしてマージする |

> **L10 の申請は承認までのリードタイムが特に長い。** レッスンの分量は10分だが実時間は数日をまたぐ。シリーズ内で完結させる原則（1.5）に従いここに置き、早めの着手はキックオフの口頭案内で促す。
>
> **L11 で collaborator の追加方法を紹介する**（Settings → Collaborators から username で追加 → 相手が承認）。ハンズオンで相方をレビュアーとして招く操作がこれに当たる。**ただし「やってみる」にはしない** — 自己学習では**招待する相手がいない**ため。自分のリポジトリでは admin なので操作自体は可能だが、練習のために同僚を招くのは不自然になる。やり方を見せるだけに留め、実際の操作はハンズオン当日に行う。
>
> L12 の push 対象を README.md にすることで、Markdown が整形表示される体験をここで通す。**Markdown の文法は見出し・箇条書き・コードブロック程度に留め、専用レッスンは作らない**（→ 11.7）。

### ⑤ Pythonシリーズ

| ID | レッスン | slug | 時間 | 学習目標 | やってみる |
|---|---|---|---|---|---|
| L16 | Pythonとは何か | `python/overview/what-is-python` | 10分 | Pythonが何に向くか、社内での使いどころが分かる | 自分の業務で自動化したい作業を1つ書き出す |
| L17 | Python環境のセットアップ | `python/setup/install` | 15分 | **uv を導入し、プロジェクトの実行環境を作れる** | 申請を出す →（承認後）`uv --version` を実行する |
| L18 | はじめてのスクリプトを動かす | `python/setup/first-script` | 15分 | スクリプトファイルを作って実行できる | `hello.py` を作って実行する |
| L19 | 変数と型 | `python/basics/variables-and-types` | 15分 | 主要な型を使い分けられる | 3つの型の変数を作って表示する |
| L20 | 条件分岐とループ | `python/basics/branch-and-loop` | 15分 | `if` と `for` で処理を分岐・反復できる | 1〜10のうち偶数だけ表示する |
| L21 | リストと辞書 | `python/basics/list-and-dict` | 15分 | リストと辞書にデータを入れて取り出せる | 業務項目を辞書にして表示する |
| L22 | 関数とモジュール分割 | `python/intermediate/function-and-module` | 15分 | 処理を関数に切り出し、別ファイルから呼べる | 処理を1つ関数化する |
| L23 | ファイル読み書き（CSV / JSON） | `python/intermediate/file-io` | 15分 | **CSVを読み込み、結果を JSON で書き出せる** | サンプルCSVを読んで JSON に変換する |
| L24 | 外部ライブラリを使う | `python/intermediate/libraries` | 15分 | **pandas を導入し、CSVの読み込みと基本的な集計ができる** | pandas でサンプルCSVを読み、行数と列名を表示する |

> L16 の「やってみる」が、本計画で**自分の業務と接続する唯一の箇所**になる（ハンズオンは共通データを使うため）。
>
> **L17 は uv を使う。** Anaconda は2026年末まで社内で使用できるが、主流が uv に移りつつあるため本トレーニングでは uv に統一する（→ 11.13）。ライブラリの追加も uv 経由で行うため、L24 の pandas 導入も uv で扱う。
>
> **L17（環境構築）が L18（はじめてのスクリプト）より先。** Python が入っていない状態でスクリプトを実行することにならないよう、前版の並びを入れ替えた（→ 11.16）。この結果、概要コースは L16 の1本、環境構築コースは L17・L18 の2本になる。
>
> L24 は **pandas の超基礎のみ**。読み込みと基本的な集計に絞り、DataFrame の詳細には踏み込まない。ハンズオンでは pandas を使っても標準 `csv` を使ってもよい。

### ⑥ ツール開発実践シリーズ

| ID | レッスン | slug | 時間 | 学習目標 | やってみる |
|---|---|---|---|---|---|
| L25 | ハンズオンの準備 | `build/prep/checklist` | 10分 | 当日の環境が整っており、リポジトリとペアが確定している | テンプレートリポジトリを複製し、Python が動くことを確認する |
| L26 | 組分けスクリプトを作る（1日目）☆ | `build/workshop/grouping-script` | 60分 | Part 1 を完成させ、PR → レビュー → 承認 → マージまで通せる | （当日ハンズオン全体） |
| L27 | 応用課題に挑む（2日目）☆ | `build/workshop/advanced-challenge` | 60分 | Part 2 に取り組み、自分のコードを自分の言葉で説明できる | （当日ハンズオン全体） |

> L25 では**課題の内容は明かさない**。環境確認・リポジトリ複製・ペア確認に限る。
>
> L26 と L27 の間は 3〜4日空ける。この間の自主考察が設計の中核なので、レッスンを分けて曼陀羅上でも2回であることを表現する。

---

## 7. 時間予算

| シリーズ | レッスン数 | 時間 |
|---|---:|---:|
| ① はじめに（L01・集合） | 1 | 10分 |
| **集合（キックオフ）小計** | **1** | **10分** |
| ① はじめに（L02・環境準備） | 1 | 15分 |
| ② AI | 1 | 15分 |
| ③ Git | 6 | 85分 |
| ④ GitHub | 6 | 85分 |
| ⑤ Python | 9 | 130分 |
| ⑥ ツール開発実践（事前準備） | 1 | 10分 |
| **自己学習 小計** | **24** | **340分（5時間40分）** |
| ⑥ ツール開発実践（当日） | 2 | 120分 |
| **集合（ハンズオン）小計** | **2** | **120分** |
| **総計** | **27** | **470分** |

- 目標は5時間、上限6時間。自己学習は **5時間40分** で上限内に収まっている
- **残り20分が調整枠。** レッスンを追加する場合はこの枠内に収めるか、既存レッスンを削る
- 前版から VSCode レッスン（15分）が増え、L01 が集合側に移った（−10分）。差し引き自己学習は +5分、調整枠は 25分 → 20分

---

## 8. 公開とデータの方針

### 8.1 公開（2段階）

| フェーズ | 形態 | 内容 |
|---|---|---|
| 1 | 静的サイト → 社内 GitHub Pages | Markdown を HTML に変換。曼陀羅を目次として掲載。画像も同梱 |
| 2 | Azure Static Web Apps | ユーザーアカウントと紐付け、進捗・トロフィー等のゲーミフィケーション。画像は Azure Blob |

**フェーズ1の公開先は社内 GitHub Pages で確定。** アクセスすると課金が発生するが、**今回はトライアルという位置づけで許容する。** このトレーニングが成功したら、次のステップとしてフェーズ2（Azure Static Web Apps）への移行を検討する。

生成スタックは **VitePress を第一候補、うまくいかない場合は MkDocs** の二段構え（→ 未決事項1）。

### 8.2 識別子

| 種別 | 用途 | 採番 |
|---|---|---|
| `id`（`srs-` / `crs-` / `lsn-`） | 不変キー。フェーズ2の進捗・トロフィーのキー | **dx-training-studio がプログラムで採番**（本計画書では扱わない） |
| `slug` | 公開URL。`/{series}/{course}/{lesson}/` | 本計画書で定義（第5章・第6章） |

**設計意図**: slug を後から推敲しても id が不変なので、進捗データが壊れない。逆に id を URL にすると `/crs-git-4fh0j2/` となり可読性と検索性を失う。両方持つことで両立させる。

### 8.3 コース `.meta.json` の `format` フィールド

```json
{
  "format": "lecture"
}
```

| 値 | 意味 | 曼陀羅の ☆ |
|---|---|:---:|
| `self-study` | 独学 | — |
| `lecture` | **集合。トレーナーが F2F で説明する講義** | ☆ |
| `hands-on` | 集合。手を動かすワークショップ | ☆ |

- **コース単位**で持つ（レッスン単位では持たない）。学習形態はコース単位で一貫するため、付け忘れと表記ゆれを防げる
- **☆ は「集合形式かどうか」を表す。** `lecture` と `hands-on` の両方に付く。受講者は曼陀羅を見た瞬間に「集合で顔を合わせる回はどこか」が分かる
- `lecture` は前版になかった値。**L01 を F2F で実施すると決めたため追加した**（→ 11.11）

### 8.4 ハンズオン用リポジトリ

| 種別 | 内容 | 用意する人 | いつ |
|---|---|---|---|
| テンプレートリポジトリ | README（骨組みのみ）＋ サンプルCSV。**課題文は含めない** | 運営 | 事前 |
| 各自のリポジトリ | テンプレートから複製した**個人リポジトリ** | 受講者 | 事前（L25） |
| collaborator 招待 | 相方を Settings → Collaborators から追加 | 受講者 | **当日**（ペア確定後、Part 1 実装中） |
| 共通課題データ | メンバー名・休暇予定のCSV | 運営（→ 未決事項6） | 事前 |
| ペア決めスクリプト | 参加者名簿からペアを作る。**シャッフルと分割のみ**。当日デモして配布 | 運営 | 事前 |

---

## 9. 制作体制と進め方

1. 本計画書の構成に沿って、**dx-training-studio の AI エージェント（create-draft）で全レッスンの草稿を生成**する
2. 生成された草稿を Studio 上で人が磨く（技術検証・社内事情の追記・画像の差し込み）
3. 静的サイトへ変換して公開する

24レッスンを手書きすると、1本2時間として48時間かかる。同じ8月にツール開発・画像用意も並行するため、草稿生成を前提にしないと締切に届かない。

### 模範解答の用意

分量方針で決めた通り、小演習には解答例を1行、ハンズオンには完成コード一式を用意する。

小演習24本分の解答例は草稿と同時に生成できるが、**動作検証は人手**になる。ハンズオンの完成コードは Part 1 / Part 2 の両方を用意する必要がある。

**ただし Part 2 の解法は配らない。** 手法を教えない方針のため、運営が用意するのは「解けることの確認用」であって配布物ではない。配布するのは Part 1 の完成コードのみ。

### ペア決めスクリプト

当日のペア決めに使い、デモした後に受講者へ配布する（→ 6.2）。**シャッフルと分割だけに絞る**ため分量は10行程度だが、**受講者の目の前で実データを処理する唯一のコード**になるので、読みやすさと説明しやすさを優先して書く。

### F2F キックオフ

L01 はトレーナーが集合で説明する。**曼陀羅の外にある運営イベントではなく、`lecture` フォーマットのコースとして曼陀羅に載せる**（→ 11.11）。当日の口頭案内の内容は 4章 ① を参照。

### 既存コンテンツの扱い

現在 `contents/` にあるサンプル（4シリーズ・14レッスン）は**基本的にいじらない**。本計画への流用や整理が必要な場合は、実行せず提案のみ行う。

> 参考: 既存サンプルの Python環境構築コースには「Anaconda セットアップ」「uv セットアップ」の両方がある。本計画は **uv に一本化**するため、流用する場合は Anaconda 側を落とす。

---

## 10. 未決事項

| # | 項目 | 影響 |
|---|---|---|
| 1 | 静的サイト生成スタック（VitePress / MkDocs） | 8月の「ツール開発」マイルストーンの中身。**公開先は社内 GitHub Pages で確定済み**（8.1） |
| 2 | 「自己学習のトリガーをかけられるようにする」（9/11）の具体 | 受講者への展開手順。**各種申請のリマインド**をここに載せられる（→ 3.5、11.10） |
| 3 | フェーズ1での自己学習の完了記録方法 | 静的サイトのため自己申告になる想定 |
| 4 | 社内AIチャットツールの正式名称・利用ガイドライン | L03 の記述。ハンズオンで AI 使用可としたため、**どこまで使ってよいかの線引き**も要る |
| 5 | `<details>` の折りたたみが静的サイト生成で潰れないか | 潰れる場合、解答例の見せ方を代替手段（別ページ・注釈）に切り替える必要がある。生成スタック確定後に要検証 |
| 6 | **共通課題データ（メンバー名・休暇予定CSV）の作成** | Part 1 / Part 2 の難易度がデータの人数・休暇の入り方で変わる。Part 2 は**「解が存在する」ことを保証したデータ**にする必要があるので、運営が先に解いて確認する |
| 7 | ハンズオンの実施日程（1日目・2日目の具体日） | 間隔は3〜4日と決めたが、具体日は未定。未決事項2（自己学習トリガー）と連動する |
| 8 | **各種申請の実際のリードタイム** | VSCode・Git・社内GitHub・uv の4つ。特に社内GitHubアカウントが長い。実測値が分かればキックオフでの案内を具体的にできる |
| 9 | **`format` に `lecture` を追加する実装** | `lib/schema.ts` の Zod スキーマと、曼陀羅の ☆ 表示ロジックに手が入る。dx-training-studio 側の対応が要る（本計画書は仕様の提案までで、実装は行わない） |
| 10 | **キックオフ（L01）の実施時間** | レッスンの分量は10分だが、F2F セッションとしては説明・質疑を含めてより長くなる想定。実際の枠は未定 |
| 11 | **VSCode + GitHub Copilot を将来どう扱うか** | AIシリーズを育てる際に、`start→ai` の辺を DX入門から環境準備コースへ張り替えるかの判断が要る。現時点では踏み込まない（→ 4章 ②） |

**仮置きしている箇所**: ハンズオンの題材は「課の10人を週ごとに3組に分ける」を確定としたが、**人数・組数・休暇の条件は仮**。未決事項6 の課題データ作成時に確定させる。

---

## 11. 決定の根拠と却下した案

後から経緯を再現できるよう、主要な分岐と却下理由を残す。

### 11.1 ハンズオンの題材と形式

- **採用**: 共通課題を解く Python スクリプト（組分けスクリプト）。AoC 風に Part 1 / Part 2 へ段階化し、間に3〜4日の自主考察を挟む
- **却下**: 各自が業務課題を持ち込む案 — 環境差とスコープ差で2時間の完走率が読めず、講師のサポート負荷が人数分に膨らむ（前版から継続）
- **却下**: 段階化せず応用込みで一括出題する案 — 「同じメンバーと連続しすぎない」は組合せ最適化（Social Golfer Problem の変種）であり、初心者が2時間で書けるものではない。**Part 1 まででツールとして動く**構成にしないと、完成しなかった人が手ぶらで帰ることになる
- **却下**: Part 3（全期間でペアを均等化）まで用意する案 — 2日しかないので到達しない。用意しても使われない

### 11.2 自分の業務データを使うか

- **採用**: 運営が用意した共通データのみ。受講者自身のデータは使わない
- **却下**: 自分の課のメンバー名・休暇予定を持参させる案

  **理由**: 課題を1日目まで伏せる方針と衝突する。自分のデータを用意させるには CSV の列構成を事前に開示する必要があり、開示すれば課題が部分的に漏れる。加えて、**今回のスクリプトがそのままの形で実務に使えるケースはほぼない**ため、自分のデータにするメリットが労力に見合わない。

  **代償**: 設計原則の「用意されたお題をなぞらせない。実際に使える道具を作らせる」とは緊張が残る。これは**意図的に受け入れたトレードオフ**であり、自分の業務との接続は L16 の「やってみる」（自動化したい作業を書き出す）に残す。

- **却下**: 共通データで解いた後、自分のデータに差し替える案 — 2日間しかないため差し替えの時間が取れない

### 11.3 ハンズオンの出力形式と外部ライブラリ

- **採用**: JSON 出力。外部ライブラリは L24 で **pandas の超基礎**
- **却下**: Excel 出力（`openpyxl`） — 記法に癖があり初心者がつまずく。加えて **CSV → Excel は形式がほとんど変わらず**、変換した実感が薄い
- **却下**: Markdown レポート → `tabulate` で表生成 — tabulate は一般的でなく、覚えても他所で使えない
- **却下**: Markdown レポート → `matplotlib` でグラフ生成 — matplotlib も記法が独特。加えて日本語ラベルのフォント設定でつまずく
- **却下**: Markdown → MkDocs で HTML 化 → リポジトリに配置 — **ビルド成果物である HTML をリポジトリに置くのは不自然**。README.md なら GitHub がそのまま整形表示するので、変換工程を挟む必要がない
- **却下**: **受講者の成果物を** GitHub Pages で公開して URL を共有する案

  **理由**: 公開の自動化には GitHub Actions の説明が必要になり、自己学習に GitHub Actions のレッスンを足すことになる（調整枠20分では足りない）。加えて社内 GitHub Pages はアクセスすると課金が発生するため、受講者それぞれが公開して回る形は避けたい。

  **教材サイト自体の公開先は社内 GitHub Pages で確定している**（8.1）。運営が1つ公開するのと、受講者8人がそれぞれ公開するのとでは、必要な説明量も課金の広がり方も異なる。

### 11.4 ペアの役割

- **採用**: 2人一組。**相互レビューの相手としてのみ**。実装は各自独立。各自が自分のリポジトリを持ち、相方を collaborator に招待する
- **却下**: 交代制ペアプロ（課題ごとに実装者とレビュアーを交代） — **ペアプロをトレーニングに組み込むのはハードルが高い**。進行管理の難度が上がり、片方のペースに引きずられる
- **却下**: 分担制（機能を2分割して並行実装し、後で結合） — 関数のインタフェースがずれたときの結合デバッグに60分は耐えられない。初心者ペアが最も高い確率で時間を溶かす箇所
- **却下**: branch protection（承認1件必須）で承認を強制する案 — 設定の説明に当日の時間を取られる。**レビューを飛ばす動機が受講者側にない**ため、運用ルールとして伝えるだけで足りる
- **却下**: 権限を read に絞るため、リポジトリを org 配下に置く／可視性を internal にする案 — GitHub の granular なロールは Organization 所有リポジトリの機能で、個人リポジトリでは read を選べない。ただし**相互レビューの相手に write が付くことの実害がない**ため、権限を絞るためだけに構成を変える必要はない

### 11.5 発表の形式

- **採用**: 全員が各自2分ずつ発表。定員8人（4組）。**自分のコードを自分で説明できることを要件にする**
- **採用**: **未完成でも発表する。** できたところまでと、詰まった箇所を共有する。設計原則（第11回）の「未完成での参加を許容することを明示する」に沿う。これにより Part 2 が解けなかった場合も2日目が成立し、**「解けた人だけの場」にならない**
- **却下**: 1組だけ代表発表 — 応用課題の手法を教えない方針にしたため**解法が発散する**。全員分に見る価値が出た
- **却下**: 各自3分（計24分） — 8人で24分となり、Part 2 の実装時間を圧迫する。2分に縮めることで実装に25分確保した
- **却下**: 定員10人（5組） — 発表に30分必要になり、2日目の時間割が崩れる。**回数を増やすほうが1回を大きくするより制御しやすい**

**AI 使用可とセットで機能する。** AI に丸投げしても発表で説明を求められるため、理解せずに済ませる導線が塞がる。設計原則の「理解できないものは作るな」を、禁止ではなく**運用ルールとして実装**した形。

### 11.6 応用課題の手法を教えるか

- **採用**: 教えない。各自が数日かけて工夫する。仲間との相談・AI への質問は自由
- **却下**: 想定解法（シャッフル＋リトライ）をレッスンで扱う案 — 全員が同じ解法になり、発表の価値が消える。設計原則の「運営は代わりに作らない」「分からないことは自分で調べる導線を先に渡す」にも反する

この方針の結果、**AIシリーズがハンズオンの実質的な前提になった**（3.3 参照）。自主考察期間の主要な調べ手段が AI になるため。

### 11.7 Markdown をどう扱うか

- **採用**: 専用レッスンは作らない。**L12「push と pull」の push 対象を README.md にする**ことで、整形表示される体験だけ通す
- **却下**: Markdown 専用レッスンを新設する案（15分） — 完成物が Python スクリプトになり、README は補足程度になったため、文法を体系的に教える必要がなくなった
- **却下**: はじめにシリーズに置く案 — はじめにシリーズは最初の関門を軽く保つ設計であり、文法レッスンを足すと重くなる

### 11.8 `.gitignore` の置き場所

- **採用**: L07「最初のコミット」に含める（時間は15分据え置き）
- **却下**: 独立レッスンにする案 — 丁寧になるが +10〜15分で調整枠が削られる。`.gitignore` は「何をコミットするか決める」文脈の一部なので、コミットのレッスンから切り離すほうが不自然

**必要になった理由**: ハンズオンの成果物は Python スクリプトであり、**スクリプトが生成する JSON はコミットしない**方針を採ったため。生成物をリポジトリに置かないという原則を、実際に手を動かして守らせる。

### 11.9 ハンズオン2日の間隔

- **採用**: 3〜4日
- **却下**: 1週間 — 思考時間は十分だが、**1日目の内容を忘れ、熱量が下がる**。設計原則の「『作りたい』瞬間を逃さない」に反する
- **却下**: 2週間 — 上記がさらに強く出る。再立ち上げに時間を取られる

### 11.10 環境準備の案内をどこに置くか

- **採用**: **シリーズごとのトピックは、リードタイムがかかってもそのシリーズ内で完結させる**（→ 1.5）。Git申請は L06、社内GitHubアカウント申請は L10、uv は L18。**VSCode だけが横断的なため、はじめにシリーズに置く**
- **却下**: L01 で Git 申請を出させる案（前版の設計）

  **理由1**: L01 の学習目標は「曼陀羅の見方と進め方が分かる」だが、演習が「申請を出す」では**目標を一切検証できていない**。演習は目標の達成を確認するものであるべき。

  **理由2**: **Git が何かを知る前に Git の申請を出させることになり、文脈がない。**

- **却下**: L01 に Git申請のリードタイムを「情報として」載せ、それを踏まえて順番を決めさせる案

  **理由**: **Git の話は Gitシリーズでやるべき。** L01 に Git の事情を書き始めると、同じ理由で GitHub アカウント申請も uv も載せることになり、**進め方のレッスンが環境構築の目次になる**。

- **却下**: リードタイムの長い申請をすべて、はじめにシリーズに集める案

  **理由**: 「時間がかかるから先頭に置く」を認めると、はじめにシリーズが全シリーズの環境構築置き場になる。**シリーズの独立性が崩れ、受講者がどのブランチから始めても成立するという曼陀羅の前提も崩れる。**

**口頭とレッスンの役割分担**: リードタイムのある申請は、**キックオフで口頭案内する**。レッスン本文には書かない。口頭は「いつ動くべきか」を伝えるのに向き、レッスンは「どう操作するか」を残すのに向く。前版は両者を L01 に混ぜていた。

**残るリスク**: キックオフの案内を聞き流した受講者は、学習期間の終盤で申請のリードタイムに当たる。自己学習期間は3週間あるため致命的ではないが、期間開始時のリマインド（→ 未決事項2）で保険をかけられる。

### 11.11 VSCode レッスンの置き場所と `format` の拡張

- **採用**: はじめにシリーズを**2コースに分割**。`DX入門コース`《lecture》☆ と `環境準備コース`《self-study》
- **採用**: `format` に **`lecture`** を追加。値は `self-study` / `lecture` / `hands-on` の3つ。**☆ は「集合形式かどうか」を表し、`lecture` と `hands-on` の両方に付く**
- **却下**: VSCode レッスンを DX入門コース内に置く案

  **理由**: L01 は F2F 講義、VSCode は自己学習。**「学習形態はコース単位で一貫させる」という既定方針に反する。** VSCode は申請にリードタイムがあるため当日その場では終わらず、集合扱いにはできない。コースに ☆ が付くと VSCode まで集合に見える。

- **却下**: VSCode 設定もキックオフの場で一緒にやる案 — 申請のリードタイム上、当日は完了できない
- **却下**: L01 を曼陀羅の外にある運営イベントとして扱い、`format` を拡張しない案 — 受講者から見て**集合で顔を合わせる回が曼陀羅に現れない**のは不親切。`lecture` を足せば、キックオフとハンズオンが同じ ☆ で表現でき、全体像が1枚で伝わる

**分岐は Git だけが環境準備コースを経由する。** AI と Python は DX入門コースから直接分岐する。

- **却下**: 3方向すべてを環境準備コースの後ろに置く案

  **理由**: VSCode が全ブランチのゲートになり、**承認待ちの数日間、受講者が何もできなくなる**。AI（15分）と Python（130分）の145分ぶんの進行機会を、3週間の期間の頭で失う。

  AI は社内AIチャットがブラウザで完結するため VSCode を必要としない。Python もエディタは何でもよく、VSCode は「あると便利」に留まる。**必須なのは Git だけ**（実務で Git を VSCode から操作するため）。

- **却下**: 環境準備コースを分岐から外し、脇に置く案 — 出口の辺がなくなり**行き止まり**になる。飛ばして修了できてしまう

**環境準備コースは必須のまま保たれる。** Git → GitHub → build の経路上にあるため、Python と AI だけ進めて修了することはできない（→ 3.3）。

### 11.12 Git を CLI で教えるか VSCode で教えるか

- **採用**: **CLI を主とし、同じ操作を VSCode でどうやるかをスクリーンショットで併記する**
- **却下**: VSCode を主とし CLI を併記する案 — 実務に近いが、**ボタンの裏で何が起きているかが見えない**。設計原則の「表面的な使い方ではなく原理を教える」に反する
- **却下**: VSCode のみ — L05「Gitの三大エリア」で教えた概念と操作が対応しなくなる。エラー時に検索もしにくい
- **却下**: CLI のみ（前版どおり） — 実務では VSCode から Git を操作することが多く、対応付けを受講者任せにすると学習が実務に繋がらない

**併記の位置づけ**: GUI のボタン位置はツールが変われば無効になるが、Git の概念とコマンドは残る。**原理を CLI で押さえたうえで、実務の入口として VSCode を示す**という順序にする。

### 11.13 Python 環境構築に何を使うか

- **採用**: **uv**
- **却下**: Anaconda — 社内では2026年末まで使用できるが、**主流が uv に移りつつある**。トレーニングで教えたものが期限付きだと、受講者は学び直しになる
- **却下**: 両方を並べて選ばせる案 — 環境構築で分岐すると、以降のレッスンの手順も分岐する。初心者向けトレーニングで環境が2通りあると、詰まったときの切り分けが難しくなる

ライブラリの追加も uv 経由で行うため、L24 の pandas 導入も uv で扱う。

### 11.14 ペアの決め方と配布物

- **採用**: **当日、運営が用意したペア決めスクリプトを実行してデモし、その結果でペアを確定する**
- **採用**: そのスクリプトを受講者に配布する。**ただしシャッフルと分割の部分だけ**に絞る

**採用理由**: 講師が恣意的に決めるより納得感がある。加えて、**今日作るのと同じ種類のツールが、その場の実データ（参加者名簿）で動く**ところを見せられる。設計原則の「実際の操作画面を見せる」を、教材の中ではなく**その場の実務**で成立させている。

11.2 で自分の業務データを使わないと決めた結果、トレーニング中に実データが動く場面がなくなっていた。**このデモがその代償を部分的に埋める。**

- **却下**: フル版（CSV読込・除外・JSON出力を含む）を配る案 — 「シャッフルしてスライスで分ける」は Part 1 の中核であり、フル版だと **Part 1 が調整作業になる**。CSV読込・除外・JSON出力はペア決めに本来不要なので、無理なく排除できる
- **却下**: デモのみで配布しない案 — メタ的な効果は残るが、参考材料としての価値を捨てることになる。Part 1 は「全員が動くツールを完成させる」工程であり、難易度で選別する場ではないので、完走率が上がるほうを取る
- **却下**: 配布を2日目に回す案 — Part 1 の完走率が下がる。Part 1 が終わらないと Part 2 の自主考察が始められない

**Part 2 は漏れない。** 評価関数を書いて条件を満たすまで試すという発想は、一回きりのペア決めには不要だからである。

### 11.15 collaborator 招待をレッスンの課題にするか

- **採用**: **L11 で「やり方の紹介」に留める。「やってみる」にはしない**
- **却下**: L11 の演習として実際に招待させる案

  **理由**: **自己学習では招待する相手がいない。** 自分のリポジトリでは admin なので操作自体は可能だが、練習のために同僚を招くのは不自然で、招かれた側にも作業が発生する。

  実際の操作はハンズオン当日、ペア確定後に行う。Settings → Collaborators から追加するだけなので、当日その場の説明で足りる。

- **却下**: レッスンで一切触れず、当日すべて説明する案 — 当日の冒頭10分に説明が集中する。一度見たことがある状態にしておけば、当日は確認で済む

### 11.16 Python概要コースと環境構築コースの分け方

- **採用**: 概要コースは **L16「Pythonとは何か」の1本**。環境構築コースを **L17「Python環境のセットアップ（uv）」→ L18「はじめてのスクリプトを動かす」の2本**にする
- **却下**: 前版の並び（概要コースに「はじめてのスクリプト」を置き、環境構築をその後にする）

  **理由**: **Python が入っていない状態でスクリプトを実行することになる。** 前版では曖昧なまま通っていたが、**uv に申請が必要と決めたことでこの矛盾が実害を持つ**。承認前に到達した受講者には実行環境がない。

  「Pythonとは何か」は環境がなくても読めるが、「スクリプトを動かす」は環境がないと成立しない。**環境を要するかどうかでコースを分ける**ほうが、コースの意味も明確になる。

レッスン数（9）と時間（130分）は変わらない。コース内訳が `概要 2/25分・環境構築 1/15分` から `概要 1/10分・環境構築 2/30分` に移る。

### 11.17 前版から継続している決定

以下は前版（`training-plan-20260804.md`）の決定をそのまま引き継ぐ。

| 論点 | 採用 | 主な却下理由 |
|---|---|---|
| 分量の2層化 | 自己学習24レッスンすべて必須（層分けなし） | 基礎は枠内で十分カバーでき、層を分ける複雑さが見合わない |
| AIシリーズの規模 | 1レッスン | 現時点では「使えれば良い」。今後育てる前提でシリーズは独立させた |
| ブランチをどのシリーズに置くか | GitHubシリーズ | **シリーズ間の辺は同一ペアにつき全体で1本**しか張れないため、Gitに残すと `Gitブランチ → GitHub PR` の依存が表現できず、ブランチ未習のまま PR コースに入れてしまう。加えて「ブランチ → push → PR → マージ」は一連で理解する内容 |
| 合流先シリーズの名称 | 「ツール開発実践シリーズ」 | 「卒業試験」は評価される場という含意が心理的ハードルになる |
| 学習形態の保持場所 | コース `.meta.json` の `format` | tags は自由語彙で表記ゆれる。レッスン単位は付け忘れのリスクだけ増える |
| 演習の入れ方 | 全レッスンに5分以内の「やってみる」 | コース末にまとめると、どこで分からなくなったか本人にも特定できない |
