# 引き継ぎ: dx-training-studio

**次の主線は「GitHub or Python シリーズで検証実行」**（→ 2章）。**`dx-training-review` の初回実行は独立して先に回せる**（→ 2章末）。ほかに残っているのはツール解禁と `connection-profiles` の2つだけで、どちらも急がない。

本文書は `handoff-agent-port-followup.md`（EBEX agent 移植系統）と `handoff-dx-training-create.md`（スキル系統）を 2026-08-13 に統合したもの。**完了した作業の経緯は書かない** — 記録は `openspec/changes/archive/<日付>-<change名>/` の `design.md` と `tasks.md` が正本（⚠ ただし追跡外。→ 5.1）。

---

## 1. 残っている仕事

```
[次] review スキルの初回実行（Git基礎シリーズ）    ← 独立して回せる。2章末
       新設スキルの実地確認。ここで初めて使うもの:
       深さ2択 / 検査項目 A〜Q / 心の声ウォークスルー /
       指摘ファイルの blocking 判定

[次] 検証実行（はじめにシリーズ・ペイン4・Sonnet 5） ← 主線。2章
       ここで初めて使うもの: 名前確認ゲート /
       社内コンテキスト3段梯子 / context-outbox.json /
       画像コメントの出力保証 / 禁止事項4項目 / 模範解答1本 /
       ペイン4 実行そのもの / 軽量モデルでの品質

[済] ペイン4 へのツール解禁                        2026-08-13 完了
       dx-training-create の frontmatter に tools: を宣言した。
       「1度回して観測してから」の順序は取れなかった——ツールが
       無いと declaredTools=[] でファイル操作が一切できず、
       観測に足る動きが起きないため（→ 2章）

[後] connection-profiles                          会社持ち込みの直前で可。3章

[後] 模範解答の2本目（→ 4章。1本目は昇格済み）
     空欄埋め（社内側の埋め作業フロー。別系統）
```

2026-08-10〜13 に、EBEX agent の移植・作業ファイル置き場の再設計・`workspace/` 廃止・`contents/` 書込ゲート・スキル改善4本（プレビュー / create / plan 命名 / コンテキスト梯子）・名前確認ゲート・run レイアウト調整（`reviews/` と曼陀羅の図）・Git シリーズ6本の原稿更新パス・**`dx-training-review` の新設・create のガードレール追加・用語統一（穴→空欄）・模範解答1本の昇格・旧スキル削除**までが完了している。

### リポジトリの状態

ブランチ **`dx-training-studio3`**。2026-08-13 の作業は **`224a366` までコミット済み**（名前確認ゲート → Git シリーズのリネームと原稿更新 → review スキル新設 → 用語統一 → VSCode 画像コメント → 模範解答昇格）。**Git シリーズ6本の原稿と `.meta.json` もコミット済み**（8章の該当行は解消）。

**未コミットで残っているのは旧スキル2本の削除**（`.claude/skills/____create-draft/` / `____create-structure/`）。ステージ済みだがコミットはされていない。

⚠ これらのコミットは**こちらから `git commit` を叩いていない**。6章の「勝手にコミットが生まれる」現象が今日も繰り返し起きた（あるいは別ツールでの手作業）。**作業の区切りで `git log` を見る運用を続けること。**

---

## 2. 次の主線: 検証実行

`dx-training-create` を回し、改善が効いたかを見る。**これがフェーズ8「ふりかえり」の入力になる。**

**次回の条件は「はじめにシリーズ / Studio ペイン4 / Sonnet 5」**（2026-08-13 に決定）。従来の Claude Code + 強いモデルとは2つ変わるので、**品質の劣化が改善の効果と混ざる**点に注意して観測する。

はじめにシリーズを選ぶ利点が2つある。**曼陀羅の辺 #2 が閉じられる**こと（Git基礎シリーズの `cross_series_prev` は「はじめにシリーズ / 開発環境準備コース」の作成を待っている提案のまま。相手ができれば人が Studio 上で張れる）と、**シリーズ冒頭なので前レッスンへの参照が少ない**こと（軽量モデルが苦手な文脈またぎの整合が出にくく、初回として安全側）。

### 回す前に確認すること

- **計画書のシリーズ名・コース名。** AI・GitHub・Python は「完全マスター」表記のまま（Git だけ「Git基礎シリーズ」に改めた）。フェーズ1の**名前確認ゲートで目に入る**ので、そこで決めれば足りる。`slug` は表示名と独立（計画書 §8.2）なので、名前を変えても公開 URL には影響しない
- **試作ディレクトリ。** `contents/` の `（old）` 付き4本は退避または削除を予定（人がやる）。残っている場合、新しく作る「はじめにシリーズ」と `はじめにシリーズ（old）` は別名なので衝突はしないが、一覧が紛らわしい
- ⚠ **ペイン4 では `.meta.json` が書けない**（アプリ管理ファイル）。`order` と `target` が入らないので、**シリーズ完成後に人がペイン1・2 のドラッグで並べ直す**。フェーズ7の曼陀羅で「書き込めなかった」と出るのは正常

### 観測すること

| 観測対象 | 見かた |
|---|---|
| 明文化した規則が守られたか | レッスン番号の生参照・用語の揺れ・表の不使用・出典の URL 直書き・単純化の明示漏れが、初回実行より減ったか |
| 新しく崩れたものはないか | 明文化を増やしたぶん、守りきれず落ちる項目が出ていないか。落ちたなら「規則が多すぎる」のサインで、`references/` の整理を検討する |
| コース単位への縮小の効き | 1コース ≈ 15分の見積もりが実態と合うか。承認ゲート・レビュー採否に人が付き合えたか |
| run またぎの引き継ぎ | 同一シリーズの2コース目で、用語表とたとえが実際に引き継がれたか |
| 社内コンテキストの3段梯子 | 段1（DB 検索）が実行環境で届くか。届かないときに段2→段3へ自然に降格するか。`context-outbox.json` が書かれ、登録の促しが出るか |
| 名前確認ゲート | 提示された名前でそのまま進めたか。止めて計画書を直す判断が要ったか |
| **画像コメントの出力保証**（今回追加） | 図を置くと決めた節に必ずコメントが出たか。画像前提の本文だけを書いて落とす事故が消えたか |
| **実在アプリ画面の書き分け**（今回追加） | 純再現／注釈付きを書き分けたか。生成物に余計な説明ラベルが混ざる頻度が下がったか |
| **模範解答の効き**（1本になった） | トンマナと密度が模範解答に寄ったか。逆に題材が違うのに引きずられていないか |
| **禁止事項4項目**（今回追加） | たとえ話に属性の型当てや禁止題材が出ないか。実在名の創作が消えたか |
| **ペイン4 実行**（初） | 宣言した tools で足りたか（不足したツール名を控える）。確認ダイアログの頻度が作業を止めないか。自己レビューが `run_isolated_task` で成立するか |
| **Sonnet 5 の品質**（初） | フェーズの飛ばしが増えないか（規則の量に耐えるか）。1レッスンの密度と語り口が模範解答から離れないか。**劣化が見えたら、規則を減らす前にモデルを戻して切り分ける** |

### レビュー運用は変更不要

per-lesson 独立レビュー・指摘のみ・本文の順に初学者反応・採否を執筆側がファイルに残す、というフェーズ6の設計は初回実行でそのまま機能した。レビュー役が指示されていない実機検証を自発的に行い最重要の指摘を拾った実績もある。**この設計への変更提案はない。**（これは create 内部の自己レビュー。原稿全体を見る `dx-training-review` は別物）

### `dx-training-review` の初回実行（検証実行と独立に回せる）

2026-08-13 新設。**Git基礎シリーズを対象に、まず「整合チェック」で回すのが最初の一手。** 検証実行を待つ必要はない。

- 起動時に聞くのは**範囲と深さの2つだけ**。深さは「整合チェック（推奨・短時間）」「公開前レビュー（時間がかかる）」の2択で、既定は前者。**対話に「層」などの内部語を出さない設計**なので、そこが崩れていないかを見る
- 検査項目コードは **A〜J（整合チェック）/ K〜Q（公開前レビュー）**。指摘は必ずコードと名称を併記する
- 出力は `contents-plan/runs/<yyyymmdd>-review/`（`summary.md` + `findings/`）。**原稿は直さない**——採否は人

**実行前に分かっている想定指摘**（機械検証を手で回した結果。誤検出の判定材料にする）:

| 項目 | 予想される指摘 |
|---|---|
| B: frontmatter | `status` は6本中いくつか `done` に変更済み。残りが `in_progress` なら公開前レビュー時に blocking |
| C: 画像 | 「変更を取り消す」「履歴と差分を読む」の VSCode 節は**コメント追加済み・画像生成はこれから**。未挿入として出るのが正しい |
| D: 社内データ | 「Gitのインストールと初期設定」に空欄4件 |
| A / E | 名前は全一致・裸 URL ゼロ。**ここに指摘が出たら誤検出** |

**観測すること**: 誤検出の量（多いなら references の検査手順を締める）／心の声ウォークスルーが本文を読まずに成立するか／blocking と advisory の線引きが実用的か。改善点はスキルの `references/` に反映する。

---

## 3. connection-profiles（会社持ち込みの直前で可）

決定済みの骨子。実装は未着手。

- 3 プロファイル: **private**（家・Anthropic 直・sonnet/opus）/ **development**（会社無償・gpt-5-nano 等・月 6M ゲートウェイ縛り）/ **enterprise**（会社有償・上位モデル・日次上限）
- 1 接続先 = 1 ファイル（`profiles/`）。モデル別 route・providerParams・モデルプロファイル値を同居。env 1 本で選択。**会社リポに private を置かない**
- ループ上限はモデル別のまま（プロファイル別に締めない。軽量モデルの途中停止対策で意図的に緩い）。予算はゲートウェイ任せで、アプリ内の累計トークン会計は実装しない
- LLM SDK は見合わせ。再訪トリガーは (a) 会社 PC で社内ルートのワイヤ形式を Azure OpenAI 純正と照合したとき (b) Gemini 対応で3つ目のワイヤ形式を書く前

---

## 4. 模範解答（1本・2026-08-13 昇格）

`references/model-answer/` に **「Gitの三大エリア」1本**（`contents.md` + 出自を書いた `README.md`）。**0本の状態も正常**（spec で保証済み）なので、無い場合の経路は壊さないこと。

昇格の理由は形式の網羅（画像コメント・出典・表・コードフェンス・アスキーアート・`<details>`・言い換え型の確認問題）に加えて、**執筆原則が実物として入っていること**——懸念の先回り（「二度手間では？」）、単純化の断り、分からなくてよい範囲の明示、そして**たとえの限界を同じ比喩の中で語る**（「ゲームのセーブには『選ぶ』に当たる段はありません」）。最後のものは design-principles で最も難しい技で、ここが見本価値の中心。

**この1本に無い要素**: アラート（TIP/CAUTION）・`diff` フェンス・実在アプリ画面のプロンプト・**空欄の記法**。形式の正本は `lesson-template.md` が持つので網羅は不要だが、空欄の書きぶりだけは実例が無い（→ 2本目の判断材料）。

⚠ **模範解答はコピーであって参照ではない。** レッスン本体（`contents/Git基礎シリーズ/Git概念コース/Gitの三大エリア/`）を後から直しても追従しない。**本体に手を入れたら再コピーする**（README にも明記済み）。

**波及**: `dx-training-review` の K（トンマナ）の採点基準が「コーパス自己一貫性のみ」から「模範解答から読み取った文体 ＋ 自己一貫性」に格上げされた。次の公開前レビューから効く。

### 担わせるもの / 担わせないもの

| 担わせる（規則で書けないもの） | 担わせない（実例から読み取れないもの） |
|---|---|
| **トンマナ** — 文体・語りかけ方・断定の強さ・専門語を出す間合い | **フェーズとゲート**（AI は手順を平気で飛ばす） |
| **密度** — 1節あたりの分量・図と文の比率・たとえの長さ | **出力規約**（パス・`.meta.json`・命名。1本から一般則は導けない） |
| **空欄の書きぶり** — 「答えるべき問い」の具体度 | **制約**（既存を変更しない等。実例には「やらなかったこと」が写らない） |
| マークダウンの運用（太字の当て方・箇条書きの使い分け） | **禁止事項**（負の制約は実物から読み取れない。→ `design-principles.md`） |

**スリム化できるのは `lesson-template.md` の一部だけ。** `SKILL.md` のフェーズ・出力規約・制約は削らない——むしろ削ると初回実行で観察した「飛ばし」が悪化する。

### 昇格基準（1本目では部分的にしか使えなかった）

**次の4つを全て満たす1本**を昇格させる。

1. 人の手直しが**段落単位で発生していない**（語句の修正だけで済んだ）
2. 社内の空欄が、**中身を知らない人でも何を書けばいいか分かる**粒度になっている
3. 「やってみる」が**学習目標を検証している**（演習をこなすこと自体が到達確認になる）
4. 読み返して**冒頭で詰まらない**（時系列レビューで冒頭に指摘が出ていない）

⚠ **1本目（Gitの三大エリア）は項目2を判定できなかった**——このレッスンに社内の空欄が無いため。**空欄を含むレッスンが昇格候補になったときが、項目2の初判定**になる。ほかの3項目は機能した。

⚠ **「さらっと眺めるといい感じに見える」現象に注意。** 大量の出力は全部それなりに見える。1本ずつ明示的に判定する。

### 2本目をどうするか

判断は**次の検証実行の後**。足す価値があるのは、1本目に無い型が必要になったときだけ:

- **手順型**（申請 → 承認待ち → 承認後の実作業の3段・空欄が主役）— 1本目は概念型なので、この型の出来が悪ければ足す
- **空欄の書きぶりの実例** — 上と重なる。空欄の具体度は規則では書ききれない

**先回りして作らない。** 検証実行で「1本では足りない」と実際に困ってから。

```
references/model-answer/
  contents.md          ← レッスンの contents.md をそのままコピー（frontmatter 込み）
  README.md            ← 出自・昇格日・「本体に追従しない」注意（短く）
```

**本文に注釈を書き込まない**（注釈自体が「レッスンの形」として写る）。写してよいもの／いけないものの区別は README 側に置く。2本目を足すならファイル名で区別する（1本目が `contents.md` 固定なので、そのときに命名を決める）。

---

## 5. 作法（守らないと事故る）

### 5.1 OpenSpec

- planning home は **`dx-training-studio/`**（リポジトリルートではない）。ルートから叩くと `No OpenSpec changes directory found` / `Unknown item`
- **`openspec/changes/` は `.gitignore` されている。** アーカイブした artifacts は**このマシンにしか存在しない**。コミットされる設計の記録は **`openspec/specs/` だけ**
- ✅ **`openspec archive <change名> -y` は spec 同期まで自動でやる**（2026-08-13 に3本で実測。ADDED / MODIFIED / RENAMED すべて反映され、`Specs updated successfully.` が出る）。**手で spec を書かないこと** — 以前の「同期は手作業」という記述はこれで置き換わる
- ⚠ **先に手作業で spec を同期してしまうと `openspec archive` が使えなくなる。** delta を再適用しようとして、RENAMED の元見出しが既に無いため `Aborted. No files were changed.` で止まる。そうなった場合だけ `openspec/changes/archive/<yyyy-mm-dd>-<change名>/` へディレクトリ移動でアーカイブする。**`openspec-sync-specs` スキルはこの環境に無い**が、CLI があるので不要
- ⚠ **複数 change を続けて archive するときは順序に注意。** 後の change の delta は、**それを書いた時点の spec** に対して書かれている。先に archive した change が**新しい要件を追加**すると、後の change はその要件を知らないまま適用され、取りこぼす。2026-08-13 の用語統一で実際に発生した（guardrails が追加した要件内の「穴」が rename の対象外だった）。**archive 後に横断検索で取りこぼしを確認する**
- delta 側の要件見出しは必ず `### Requirement: <名前>`。**要件内の小見出しに level-3 を使わない** — 同期すると要件がシナリオから切り離され `validate --specs` が落ちる
- ⚠ **要件変更を伴わない作業は change にしない。** spec-driven スキーマの `validate` は「差分が最低1つ」を要求する。既存要件に成果物を合わせるだけの作業（例: 2026-08-13 の原稿更新パス）は通常作業として進め、結果を本文書に記録する。要件を捏造して通さないこと
- アーティファクトは日本語で書く

### 5.2 スキルの SSoT 作法

スキルの作成・更新には **`creating-skills` スキルを使う**（SSoT 監査とハードコード・不整合レビューがゲートになっている）。

- `SKILL.md` は 200行が目安・500行が上限。`references/` は各300行以内、100行超なら目次
- **同じ内容を2箇所に書かない。** 特に `SKILL.md` に references の本文を写さない（spec の要件でもある）
- 外部ファイル（`lib/schema.ts` 等）の内容をスキルに写さない。参照先を示す
- **模範解答から読み取れるものは明文化しない**（トンマナの細則を書くと、模範解答だけが更新されたとき食い違う）。例外は**負の制約**——良い実例をいくら読んでも「書いてはいけないもの」は復元できないので、`design-principles.md` に明文で置く

### 5.3 用語（2026-08-13 に変更）

- **人が後工程で埋める箇所は「空欄」。** 旧称は「穴」。スキルと spec は全部移行済みだが、**`docs/` の過去記録（`handoff` の履歴・`grill-me/`）と run ディレクトリには旧称が残っている**（当時の記録なので意図的に残した）。古い文書で「穴」を見たら「空欄」と読み替える
- ⚠ **「プレースホルダー」を空欄の意味で使わない。** この語は `{{XXX}}` / `<!-- XXX_START -->` の**機械が決定的に埋めるスロット**を指す既存語で、`contracts/agent-write-contract.md` と agent の完了ゲート（`lib/agent/turn-end.ts`・`lib/agent/llm/types.ts`）が「残留していたら未完了」と扱う。空欄は残っていて正常なので、**完了判定の意味が正反対**になる

---

## 6. 環境の注意（事故りやすい点）

- ⚠ **誰も `git commit` を叩いていないのにコミットが生まれ、新規ファイルが勝手にステージされる**（2026-08-06 に `7ae5a53`、2026-08-11 に `c17ce74` と `b210b7b`）。Studio か何かの自動 git 連携が疑われる。**作業途中でも勝手にコミットされうる前提で、区切りごとに `git log` を見ること**
- ⚠ **Studio 実行中は `contents/` 配下が横から書き換わる。** loader が `.meta.json` に `id` を採番し、`order` を実体に合わせて整理する。ペイン4 での画像挿入も同様。**編集前に外部変更を確認する運用で対応する**
- **dev サーバーは同一プロジェクトで1台まで**（Next 16）。既に動いていればその URL に接続して検証する。検証用に立てたら必ず止める（放置 → EBUSY ロック → 起動不能の事故あり）
- **`.gitignore` のパターンは必ず anchored**（`/images/` `/contents-plan/runs/`）。非 anchored だと任意の深さにマッチし、**Tailwind のソース走査から `components/` 配下が丸ごと落ちて UI が崩れる**（2026-08-10 に発生）。変更したら **`.next` を削除する**（Turbopack は `.gitignore` の変更を検知しない）。機構の詳細はメモリ `project-tailwind-gitignore-trap`
- **CRLF + BOM のファイルがある**（`__tests__/hooks/*.test.ts` 等）。LF 前提の文字列置換が黙って空振りするので、複数行の編集は Edit ツールを使う

---

## 7. 実行時に効く既知の事実（再調査不要）

### 7.1 名前と識別子

- **シリーズ名・コース名の出典は計画書「第5章 コース仕様」、レッスン名は「第6章 レッスン仕様」。** `slug`（公開 URL 用）も同じ2章にある。`L05` の ID・`【撮】`『【録】』の印・`☆` は名前に含めない
- **曼陀羅グラフの箱の中だけは短縮表記**（箱幅の都合）。ここから名前を取らない
- **識別子は3層**（計画書 §8.2）: `id`（不変キー・進捗データ用）/ `slug`（公開 URL）/ 表示名（ディレクトリ名）。**slug は表示名と独立なので、リネームしても URL は壊れない**
- **レッスン名に `/` が含まれるものが2本ある**（L13「リポジトリを作る / clone」・L25「ファイル読み書き（CSV / JSON）」）。ディレクトリ名では `_` に置換される（`lib/content-filename.ts` の `sanitizeFilename`）。frontmatter の `lesson` は**ディレクトリ名のほう**に合わせる

### 7.2 リネームで何が起きるか（2026-08-13 に実測）

| 対象 | 挙動 |
|---|---|
| `.meta.json` の `id` | **保たれる**（ディレクトリごと移動するため）。進捗データのキーは壊れない |
| frontmatter の `series` / `course` | **ディレクトリ名から導出される**（`lib/lesson-frontmatter.ts` の `normalizeLessonMeta`）。ずれてもエラーにはならず、読み書きのたびに上書きされる |
| Studio の rename API | シリーズ・コース改名時に**レッスンの frontmatter を同期しない**（`order` の更新のみ。レッスン改名のときだけ frontmatter を触る）。ディスク上のテキストは古いまま残るので**手で合わせる**。将来の静的サイト生成は frontmatter を直読みするため効いてくる |
| `contents/.meta.json` の `order` | loader が実体に合わせて整理する。名前が変わると**末尾へ動く**ので、並び順を保ちたければ自分で書き換える |
| `session.json` | gitignore 対象。一緒に移動するだけ |
| 画像参照 | レッスンパスを含まないため影響なし |

### 7.3 `.meta.json` と `contents/` への書込

- `id` は loader が自動採番して書き戻す。**スキルが `id` を書かないのは正しい**
- `order` には実在するディレクトリだけを書く。後から足したら親の `order` を計画書の順に直す
- ⚠ **ペイン4 の agent は `.meta.json` を書けない**（アプリ管理ファイルとして保護）。ペイン4 経由なら並べ替えは**ペイン1・2 のドラッグで人がやる**。Claude Code なら直接編集できる

**`contents/` の書込規約**（`contents-write-gate` で確定。ファイルは階層を問わず自由、予約名だけ拒否）:

| 対象 | 判定 |
|---|---|
| `session.json` / `.meta.json` | **拒否**（アプリが管理。id・表示順が壊れる） |
| `contents.md`（レッスン階層） | 許可 |
| `contents.md`（それ以外の階層） | **拒否**（偽の本文になる） |
| その他のファイル（どの階層でも） | 許可 |
| 新シリーズ・新コース・新レッスンのフォルダ | 許可。ただし**実行前に確認ダイアログ**（3階層まとめて提示） |
| レッスンより深いフォルダ / `_` `.` 始まり | 許可（確認なし） |

⚠ **この規約の当初案「Zod スキーマ検査 + 構造分類 A/B/C + 近似照合」は全部破棄した。再提案しないこと。** アプリが読込・同期のたびに frontmatter を正規化するので二重になり、確認ダイアログにシリーズ名が原文で出るので打ち間違いは人が読んで気づける。

### 7.4 作業ファイルの置き場

```
contents-plan/
├─ plans/<yyyymmdd>-<slug>.md          ← 計画書。git 追跡
└─ runs/<yyyymmdd>-<slug>/             ← create 1実行分。git 追跡外
    ├─ design-note.md
    ├─ mandala.md                      ← 曼陀羅の図（確定＝実線 / 提案＝破線）を含む
    ├─ context-outbox.json             ← 段2で聞き取った社内データ（登録待ち）
    └─ reviews/<レッスン名>.md          ← レッスンごとのレビュー
```

- **識別子はフォルダ名が持ち、ファイル名は役割だけを表す。** 範囲別ファイル名と同日再実行の連番規則は廃止済み
- **サブディレクトリに畳むのはレビューだけ**（レッスン本数に比例して増える唯一の種別）。1つしか生まれない設計メモ・曼陀羅案は直下に置く
- 既存 run（`20260811-dx-training`）も 2026-08-13 に新構成へ移行済み（レビューを `reviews/` へ移し、`mandala.md` に図を追加。`design-note.md` の出力先パスもリネーム後の名前に更新）。**spec は移行を要求していない**ので、今後この作業は必須ではない
- `runs/` が**追跡外なのは仕様**。「設計メモが git に無い＝異常」ではない
- ペイン4 の `@` 参照候補は「レッスン本文 + `contents-plan/plans/` + 最新3 run」。**読取は古い run も可能**
- 作業フォルダ（相対パスの基準）= **フォーカス中のコンテンツフォルダ**。`contents-plan/` は書込許可ルートだが基準ではないので、明示プレフィックスで書く

---

## 8. 未決事項

| 項目 | 状況 |
|---|---|
| 昇格基準の項目2 | 1本目では**判定できなかった**（社内の空欄が無いレッスンのため）。空欄を含む候補が出たときが初判定（4章） |
| 模範解答を2本にするか | 1本目は昇格済み。2本目は**次の検証実行の後に判断**（4章） |
| `dx-training-review` の実地確認 | 新設・未実行。誤検出の量と blocking の線引きが未検証（2章末） |
| AI・GitHub・Python シリーズの表示名 | **原稿作成時に再検討**。「完全マスター」表記は Git で改めたのと同じ論点を抱える（AI は1レッスン15分）。**急がない** — slug は表示名と独立なのでコストはディレクトリのリネームだけ。各シリーズの create 実行時、フェーズ1の確認ゲートで目に入る |
| Git シリーズの画像生成 | 「変更を取り消す」「履歴と差分を読む」の VSCode 節は**プロンプトコメントだけ置いた状態**。ペイン4 で生成・挿入するのが残り |
| 計画書の曼陀羅 ASCII 図の枠ズレ | 2026-08-12 の改名時から、シリーズ名が枠幅に合っていない（AI・GitHub も同様）。**名前が全部決まった時点で図ごと引き直す**のが安い。中身（辺の構造）は正しい |
| `extract-markdown-block` テストの失敗 | `contents/Python基礎シリーズ/...` を読むが、当該ディレクトリは `（old）` 付きに改名済み。しかも読み先の `session.json` は gitignore 対象で**新しいクローンでも通らない**。フィクスチャ化が要る（他は787件 green） |
| `contents-work` リネーム | `contents-plan` の方が実態（plans + runs）に合わない感覚は共有済み。参照140箇所のため保留。検証実行後に再判断 |
| `content-folder-loader` spec の旧設計記述 | 前半に `_series-order.json` / `_course.json` / 数値プレフィックスが残っている。実装は `.meta.json` の `order` で動いており乖離。直すなら別 change |
| `training-create-skill` spec 前半5件の書きぶり | `抽出元と網羅` 等が過去の作業記録に読める。`## Purpose` も実態とずれている。**中身は生きているので消さない。** 直すなら別 change |
| テスト側の型エラー8件 | `estimatedMinutes` の綴り違い等。**すべて古くからのもの**で、`tsc --noEmit` のアプリ側はゼロ。直すなら独立した掃除として |
| 退避機能が `contents-plan/` を保護していない | `toProjectRelative` がフォーカス中の `contents/` 配下しか見ないため、額縁テンプレートが実際に置かれる `contents-plan/runs/` では発火しない。必要な形は分割出力を実際に使ってからでないと決まらない |

---

## 9. 対象外（やらない）

- **空欄埋めスキル（社内側）** — 入口（3段梯子の段2・段3）は決めたが、埋め作業のフロー自体は別系統
- **社内コンテキスト DB の逆向き運用** — スキルは正本に対して**読み取り専用**。段2の聞き取りは `context-outbox.json` に退避し、正規登録は Studio 側の別工程のまま
- **Studio 側の改修**（画像コメント種別の判定、未充足の空欄の一覧表示）。**`lib/ai-image-prompt.ts` の生成品質強化も当面やらない** — 余計な説明ラベルの抑止はまずプロンプト側（`lesson-template.md` の書き分け）で効果を見る
- **`dx-training-review` に修正機能を持たせること** — 指摘のみ・採否は人、が設計の芯
- **計画書の再修正**（構成の作り直し。名前だけの限定更新は別）
- **`training-create-skill` spec の要件削除**（8章の該当行を参照）

---

## 10. 参照すべき正本

| 何 | どこ |
|---|---|
| 要件の正本（**唯一コミットされる**） | `openspec/specs/` |
| 完了した change の判断と経緯 | `openspec/changes/archive/<日付>-<change名>/`（⚠ 追跡外・このマシンのみ） |
| 2026-08-12 の20課題の決定 | `docs/grill-me/grill-me-20260812.md` |
| agent の書込制約 | `contracts/agent-write-contract.md` |
| 画像スロットの書式 | `contracts/image-slot-contract.md` |
| 執筆の質・レッスンの型・禁止事項 | `.claude/skills/dx-training-create/references/` |
| 質とトンマナの実物（模範解答） | `.claude/skills/dx-training-create/references/model-answer/` |
| レビューの検査項目と手順 | `.claude/skills/dx-training-review/references/` |
| 計画書 | `contents-plan/plans/20260805-dx-training.md`（識別子の設計は §8.2） |
| UI 崩れの機構・復旧手順 | メモリ `project-tailwind-gitignore-trap` |
| 移植の決定ロック | メモリ `project-dx-agent-port` |
